{
  "name": "sentinel-ai-workspace",
  "version": "1.0.0",
  "description": "Sentinel AI - Vehicle Intelligence & Clone Detection Platform",
  "scripts": {
    "backend-install": "cd backend && pip install -r requirements.txt",
    "frontend-install": "cd frontend && npm install",
    "install-all": "npm run frontend-install && npm run backend-install",
    "backend-dev": "cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "frontend-dev": "cd frontend && npm run dev",
    "dev": "npx concurrently \"npm run backend-dev\" \"npm run frontend-dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
